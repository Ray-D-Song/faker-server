{
  "name": "@ray-d-song/faker-server",
  "version": "0.1.0-beta.4",
  "description": "Faker.js mock server with a web interface",
  "bin": {
    "faker-server": "dist/index.cjs"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev:client": "pnpm run --filter client dev",
    "dev:server": "pnpm run --filter server dev",
    "build:node": "pnpm run --filter server build:node && pnpm run --filter client build",
    "build:worker": "pnpm run --filter server build:worker && pnpm run --filter client build && npx rollup -c rollup.config.mjs",
    "format": "oxlint --fix --fix-suggestions --fix-dangerously && prettier --write ."
  },
  "keywords": [
    "faker",
    "mock",
    "server",
    "mock-server",
    "faker-server",
    "faker.js",
    "fakerjs"
  ],
  "author": "ray-d-song",
  "license": "MIT",
  "homepage": "https://github.com/ray-d-song/faker-server#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/ray-d-song/faker-server.git"
  },
  "devDependencies": {
    "oxlint": "^0.9.10",
    "prettier": "3.3.3",
    "unenv": "^1.10.0"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "tabWidth": 2
  }
}
